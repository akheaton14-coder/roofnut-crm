"use client";
import Link from "next/link";import { DragEvent,useEffect,useState } from "react";import { CrmShell } from "@/components/crm-shell";import { useWorkspace } from "@/lib/use-workspace";
type Stage={id:string;name:string;color:string;position:number;time_limit_days:number|null};type Job={id:string;title:string;contract_value:number;next_action:string|null;workflow_stage_id:string|null;updated_at:string;clients:{first_name:string;last_name:string}|null;properties:{city:string}|null};
export default function PipelinePage(){const {supabase,organizationId,loading,userName}=useWorkspace();const [stages,setStages]=useState<Stage[]>([]),[jobs,setJobs]=useState<Job[]>([]),[workflowId,setWorkflowId]=useState("");useEffect(()=>{if(!organizationId)return;(async()=>{const {data:w}=await supabase.from("workflows").select("id").eq("organization_id",organizationId).eq("is_default",true).maybeSingle();if(!w)return;setWorkflowId(w.id);const [{data:s},{data:j}]=await Promise.all([supabase.from("workflow_stages").select("id,name,color,position,time_limit_days").eq("workflow_id",w.id).order("position"),supabase.from("jobs").select("id,title,contract_value,next_action,workflow_stage_id,updated_at,clients(first_name,last_name),properties(city)").eq("organization_id",organizationId).eq("workflow_id",w.id)]);setStages((s||[]) as Stage[]);setJobs((j||[]) as unknown as Job[])})();},[organizationId,supabase]);async function drop(event:DragEvent,stageId:string){event.preventDefault();const jobId=event.dataTransfer.getData("jobId");if(!jobId)return;setJobs(current=>current.map(job=>job.id===jobId?{...job,workflow_stage_id:stageId}:job));await supabase.from("jobs").update({workflow_stage_id:stageId,updated_at:new Date().toISOString()}).eq("id",jobId)}if(loading)return <main className="auth-loading"><span>R</span></main>;const total=jobs.reduce((sum,j)=>sum+Number(j.contract_value),0);return <CrmShell userName={userName}><div className="board-page"><div className="board-head"><div><p className="eyebrow">LIVE WORKFLOW</p><h1>Roofing Pipeline</h1><p>{jobs.length} jobs · ${total.toLocaleString()} total value</p></div><Link href={`/settings/workflows/${workflowId}`} className="board-settings">⚙ Edit workflow</Link></div><div className="kanban">{stages.map(stage=>{const cards=jobs.filter(job=>job.workflow_stage_id===stage.id);return <section className="kanban-column" key={stage.id} onDragOver={e=>e.preventDefault()} onDrop={e=>drop(e,stage.id)}><header style={{borderTopColor:stage.color}}><div><b>{stage.name}</b><span>{cards.length}</span></div><p>${cards.reduce((sum,j)=>sum+Number(j.contract_value),0).toLocaleString()}</p></header><div className="kanban-cards">{cards.map(job=>{const days=Math.floor((Date.now()-new Date(job.updated_at).getTime())/86400000);return <Link href={`/jobs/${job.id}`} draggable onDragStart={e=>e.dataTransfer.setData("jobId",job.id)} className="kanban-card" key={job.id}><div className="card-top"><b>{job.title}</b>{stage.time_limit_days&&days>stage.time_limit_days&&<span>Stuck {days}d</span>}</div><p>{job.clients?.first_name} {job.clients?.last_name} · {job.properties?.city}</p><footer><b>${Number(job.contract_value).toLocaleString()}</b><span>{job.next_action||"Add next action"}</span></footer></Link>})}<div className="drop-hint">Drop job here</div></div></section>})}</div></div></CrmShell>}
