"use client";
import {useEffect,useMemo,useState} from "react";
import {CrmShell} from "@/components/crm-shell";
import {useWorkspace} from "@/lib/use-workspace";

type Product={id:string;name:string;description:string|null;category:string;unit:string;unit_price:number;taxable:boolean;active:boolean};
const blank={name:"",description:"",category:"Roofing",unit:"each",unit_price:0,taxable:true,active:true};

export default function ProductsPage(){
  const {supabase,organizationId,loading,userName}=useWorkspace();
  const [products,setProducts]=useState<Product[]>([]),[editing,setEditing]=useState<Partial<Product>|null>(null),[query,setQuery]=useState(""),[category,setCategory]=useState("All");
  async function load(){if(!organizationId)return;const {data}=await supabase.from("products").select("id,name,description,category,unit,unit_price,taxable,active").eq("organization_id",organizationId).order("category").order("name");setProducts((data||[]) as Product[])}
  useEffect(()=>{load()},[organizationId]);
  const categories=useMemo(()=>["All",...Array.from(new Set(products.map(p=>p.category)))],[products]);
  const visible=products.filter(p=>(category==="All"||p.category===category)&&(`${p.name} ${p.description||""}`.toLowerCase().includes(query.toLowerCase())));
  async function save(){if(!editing?.name?.trim()||!organizationId)return;const payload={name:editing.name.trim(),description:editing.description||null,category:editing.category||"Roofing",unit:editing.unit||"each",unit_price:Number(editing.unit_price||0),taxable:editing.taxable!==false,active:editing.active!==false,organization_id:organizationId};if(editing.id)await supabase.from("products").update(payload).eq("id",editing.id);else await supabase.from("products").insert(payload);setEditing(null);await load()}
  async function archive(product:Product){await supabase.from("products").update({active:!product.active}).eq("id",product.id);await load()}
  if(loading)return <main className="auth-loading"><span>R</span></main>;
  return <CrmShell userName={userName}><div className="content directory product-directory"><div className="directory-head"><div><p className="eyebrow">ESTIMATING</p><h1>Product catalog</h1><p>Your reusable materials, labor, roofing systems, and upgrades.</p></div><button className="primary-button" onClick={()=>setEditing({...blank})}>＋ New product</button></div><div className="catalog-tools"><div className="catalog-search">⌕ <input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search products and descriptions…"/></div><div className="catalog-filters">{categories.map(item=><button className={category===item?"active":""} onClick={()=>setCategory(item)} key={item}>{item}</button>)}</div></div><section className="catalog-grid">{visible.map(product=><article className={!product.active?"archived":""} key={product.id}><div className="catalog-card-top"><span>{product.category}</span>{!product.active&&<em>Archived</em>}</div><h3>{product.name}</h3><p>{product.description||"No description yet."}</p><div className="catalog-price"><b>${Number(product.unit_price).toLocaleString(undefined,{minimumFractionDigits:2})}</b><span>/ {product.unit}</span></div><footer><span>{product.taxable?"Taxable":"Non-taxable"}</span><div><button onClick={()=>setEditing(product)}>Edit</button><button onClick={()=>archive(product)}>{product.active?"Archive":"Restore"}</button></div></footer></article>)}{!visible.length&&<div className="catalog-empty"><b>No products found.</b><span>Add your first roofing material, labor item, or upgrade.</span></div>}</section></div>{editing&&<div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&setEditing(null)}><section className="modal product-modal"><button className="modal-close" onClick={()=>setEditing(null)}>×</button><p className="eyebrow">{editing.id?"EDIT PRODUCT":"NEW PRODUCT"}</p><h2>{editing.id?"Update catalog item.":"Add it once. Use it everywhere."}</h2><div className="form-grid"><label className="wide">Product or service name<input autoFocus value={editing.name||""} onChange={e=>setEditing({...editing,name:e.target.value})}/></label><label>Category<input value={editing.category||""} onChange={e=>setEditing({...editing,category:e.target.value})} placeholder="Roofing, Labor, Upgrade…"/></label><label>Unit<select value={editing.unit||"each"} onChange={e=>setEditing({...editing,unit:e.target.value})}><option value="each">Each</option><option value="sq">Square</option><option value="lf">Linear foot</option><option value="sf">Square foot</option><option value="hour">Hour</option><option value="day">Day</option><option value="allowance">Allowance</option></select></label><label>Unit price<input type="number" min="0" step="0.01" value={editing.unit_price||0} onChange={e=>setEditing({...editing,unit_price:Number(e.target.value)})}/></label><label className="check-field"><input type="checkbox" checked={editing.taxable!==false} onChange={e=>setEditing({...editing,taxable:e.target.checked})}/> Taxable</label><label className="wide">Description<textarea value={editing.description||""} onChange={e=>setEditing({...editing,description:e.target.value})} placeholder="Scope, material details, warranty, or customer-facing description…"/></label></div><div className="modal-actions"><button onClick={()=>setEditing(null)}>Cancel</button><button className="primary-button" disabled={!editing.name?.trim()} onClick={save}>Save product →</button></div></section></div>}</CrmShell>
}
