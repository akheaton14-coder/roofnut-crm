"use client";
import {useEffect,useMemo,useState} from "react";
import {CrmShell} from "@/components/crm-shell";
import {useWorkspace} from "@/lib/use-workspace";

type Product={id:string;name:string;description:string|null;category:string;product_type:"material"|"labor";unit:string;cost:number;cost_tax_rate:number;profit_margin:number;unit_price:number;taxable:boolean;active:boolean};
const categories=["Roofing","Vinyl","Hardie","Gutters","Misc."];
const blank:Partial<Product>={name:"",description:"",category:"Roofing",product_type:"material",unit:"each",cost:0,cost_tax_rate:7.5,profit_margin:30,unit_price:0,taxable:false,active:true};
const money=(value:number)=>Number(value||0).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2});
function pricing(product:Partial<Product>){const cost=Math.max(0,Number(product.cost||0)),taxRate=product.product_type==="labor"?0:Math.max(0,Number(product.cost_tax_rate||0)),tax=cost*taxRate/100,costWithTax=cost+tax,margin=Math.min(95,Math.max(0,Number(product.profit_margin||0))),price=margin>=100?costWithTax:costWithTax/(1-margin/100);return{cost,tax,costWithTax,margin,profit:Math.max(0,price-costWithTax),price}}

export default function ProductsPage(){
  const {supabase,organizationId,loading,userName}=useWorkspace();
  const [products,setProducts]=useState<Product[]>([]),[editing,setEditing]=useState<Partial<Product>|null>(null),[query,setQuery]=useState(""),[category,setCategory]=useState("All"),[ready,setReady]=useState(true);
  async function load(){if(!organizationId)return;const {data,error}=await supabase.from("products").select("id,name,description,category,product_type,unit,cost,cost_tax_rate,profit_margin,unit_price,taxable,active").eq("organization_id",organizationId).order("category").order("name");if(error)setReady(false);else{setReady(true);setProducts((data||[]) as Product[])}}
  useEffect(()=>{load()},[organizationId]);
  const visible=products.filter(p=>(category==="All"||p.category===category)&&(`${p.name} ${p.description||""}`.toLowerCase().includes(query.toLowerCase()))),breakdown=pricing(editing||{});
  async function save(){if(!editing?.name?.trim()||!organizationId)return;const calculated=pricing(editing),payload={name:editing.name.trim(),description:editing.description||null,category:editing.category||"Roofing",product_type:editing.product_type||"material",unit:editing.unit||"each",cost:calculated.cost,cost_tax_rate:editing.product_type==="labor"?0:Number(editing.cost_tax_rate||0),profit_margin:calculated.margin,unit_price:Number(calculated.price.toFixed(2)),taxable:false,active:editing.active!==false,organization_id:organizationId};if(editing.id)await supabase.from("products").update(payload).eq("id",editing.id);else await supabase.from("products").insert(payload);setEditing(null);await load()}
  async function archive(product:Product){await supabase.from("products").update({active:!product.active}).eq("id",product.id);await load()}
  if(loading)return <main className="auth-loading"><span>R</span></main>;
  return <CrmShell userName={userName}><div className="content directory product-directory"><div className="directory-head"><div><p className="eyebrow">ESTIMATING</p><h1>Product catalog</h1><p>Your reusable materials, labor, roofing systems, and upgrades.</p></div><button className="primary-button" onClick={()=>setEditing({...blank})}>＋ New product</button></div>{!ready&&<div className="workflow-warning"><b>Activate product costing</b><span>Run the newest Product Costing migration in Supabase, then refresh this page.</span></div>}<div className="catalog-tools"><div className="catalog-search">⌕ <input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search products and descriptions…"/></div><div className="catalog-filters">{["All",...categories].map(item=><button className={category===item?"active":""} onClick={()=>setCategory(item)} key={item}>{item}</button>)}</div></div><section className="catalog-grid">{visible.map(product=><article className={!product.active?"archived":""} key={product.id}><div className="catalog-card-top"><span>{product.category} · {product.product_type}</span>{!product.active&&<em>Archived</em>}</div><h3>{product.name}</h3><p>{product.description||"No description yet."}</p><div className="catalog-cost-line"><span>Cost ${money(product.cost)}</span><span>{product.profit_margin}% margin</span></div><div className="catalog-price"><b>${money(product.unit_price)}</b><span>/ {product.unit}</span></div><footer><span>Final selling price</span><div><button onClick={()=>setEditing(product)}>Edit</button><button onClick={()=>archive(product)}>{product.active?"Archive":"Restore"}</button></div></footer></article>)}{!visible.length&&<div className="catalog-empty"><b>No products found.</b><span>Add your first roofing material, labor item, or upgrade.</span></div>}</section></div>{editing&&<div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&setEditing(null)}><section className="modal product-modal costing-modal"><button className="modal-close" onClick={()=>setEditing(null)}>×</button><p className="eyebrow">{editing.id?"EDIT PRODUCT":"NEW PRODUCT"}</p><h2>{editing.id?"Update catalog item.":"Build your selling price."}</h2><div className="product-cost-layout"><div className="form-grid"><label className="wide">Product or service name<input autoFocus value={editing.name||""} onChange={e=>setEditing({...editing,name:e.target.value})}/></label><label>Category<select value={editing.category||"Roofing"} onChange={e=>setEditing({...editing,category:e.target.value})}>{categories.map(item=><option value={item} key={item}>{item}</option>)}</select></label><label>Type<select value={editing.product_type||"material"} onChange={e=>setEditing({...editing,product_type:e.target.value as "material"|"labor",cost_tax_rate:e.target.value==="labor"?0:7.5})}><option value="material">Material</option><option value="labor">Labor</option></select></label><label>Unit<select value={editing.unit||"each"} onChange={e=>setEditing({...editing,unit:e.target.value})}><option value="each">Each</option><option value="sq">Square</option><option value="lf">Linear foot</option><option value="sf">Square foot</option><option value="hour">Hour</option><option value="day">Day</option><option value="allowance">Allowance</option></select></label><label>Base cost<input type="number" inputMode="decimal" min="0" step="0.01" value={editing.cost||0} onChange={e=>setEditing({...editing,cost:Number(e.target.value)})}/></label><label>Cost tax %<input type="number" inputMode="decimal" min="0" step="0.1" disabled={editing.product_type==="labor"} value={editing.product_type==="labor"?0:editing.cost_tax_rate||0} onChange={e=>setEditing({...editing,cost_tax_rate:Number(e.target.value)})}/></label><label>Profit margin %<input type="number" inputMode="decimal" min="0" max="95" step="1" value={editing.profit_margin||0} onChange={e=>setEditing({...editing,profit_margin:Number(e.target.value)})}/></label><label className="wide">Description<textarea value={editing.description||""} onChange={e=>setEditing({...editing,description:e.target.value})} placeholder="Scope, material details, warranty, or customer-facing description…"/></label></div><aside className="pricing-breakdown"><p>PRICING BREAKDOWN</p><div><span>Base cost</span><b>${money(breakdown.cost)}</b></div><div><span>{editing.product_type==="labor"?"Cost tax (labor)":`Cost tax (${Number(editing.cost_tax_rate||0)}%)`}</span><b>+ ${money(breakdown.tax)}</b></div><div className="cost-total"><span>Cost after tax</span><b>${money(breakdown.costWithTax)}</b></div><div><span>Profit at {breakdown.margin}% margin</span><b>+ ${money(breakdown.profit)}</b></div><div className="selling-total"><span>Final selling price</span><b>${money(breakdown.price)}</b></div><small>True margin means profit is {breakdown.margin}% of the final selling price.</small></aside></div><div className="modal-actions"><button onClick={()=>setEditing(null)}>Cancel</button><button className="primary-button" disabled={!editing.name?.trim()} onClick={save}>Save product at ${money(breakdown.price)} →</button></div></section></div>}</CrmShell>
}
