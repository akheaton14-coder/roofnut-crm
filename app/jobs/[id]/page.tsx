"use client";
import { ChangeEvent,useEffect,useRef,useState } from "react";import { useParams } from "next/navigation";import { CrmShell } from "@/components/crm-shell";import { useWorkspace } from "@/lib/use-workspace";
type JobDetail={id:string;title:string;stage:string;contract_value:number;next_action:string|null;client_id:string;clients:{first_name:string;last_name:string;email:string|null;phone:string|null}|null;properties:{address_1:string;city:string;state:string;postal_code:string}|null};type Activity={id:string;kind:string;body:string;occurred_at:string};type FileRow={id:string;filename:string;storage_path:string;size_bytes:number;created_at:string};
export default function JobPage(){const {id}=useParams<{id:string}>();const {supabase,organizationId,loading,userName,user}=useWorkspace();const [job,setJob]=useState<JobDetail|null>(null);const [activities,setActivities]=useState<Activity[]>([]);const [files,setFiles]=useState<FileRow[]>([]);const [note,setNote]=useState("");const [busy,setBusy]=useState(false);const fileRef=useRef<HTMLInputElement>(null);useEffect(()=>{if(!organizationId)return;(async()=>{const {data}=await supabase.from("jobs").select("id,title,stage,contract_value,next_action,client_id,clients(first_name,last_name,email,phone),properties(address_1,city,state,postal_code)").eq("id",id).single();setJob(data as unknown as JobDetail);const [{data:a},{data:f}]=await Promise.all([supabase.from("activities").select("id,kind,body,occurred_at").eq("job_id",id).order("occurred_at",{ascending:false}),supabase.from("files").select("id,filename,storage_path,size_bytes,created_at").eq("job_id",id).order("created_at",{ascending:false})]);setActivities((a||[]) as Activity[]);setFiles((f||[]) as FileRow[]);})();},[id,organizationId,supabase]);async function addNote(){if(!note.trim()||!job||!user)return;setBusy(true);const {data}=await supabase.from("activities").insert({organization_id:organizationId,job_id:job.id,actor_id:user.id,kind:"note",body:note.trim()}).select("id,kind,body,occurred_at").single();if(data)setActivities(c=>[data as Activity,...c]);setNote("");setBusy(false)}async function upload(e:ChangeEvent<HTMLInputElement>){const file=e.target.files?.[0];if(!file||!job||!user)return;setBusy(true);const path=`${organizationId}/${job.id}/${Date.now()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,"-")}`;const {error}=await supabase.storage.from("job-files").upload(path,file);if(!error){const {data}=await supabase.from("files").insert({organization_id:organizationId,client_id:job.client_id,job_id:job.id,storage_path:path,filename:file.name,content_type:file.type,size_bytes:file.size,uploaded_by:user.id}).select("id,filename,storage_path,size_bytes,created_at").single();if(data)setFiles(c=>[data as FileRow,...c])}setBusy(false);e.target.value=""}if(loading||!job)return <main className="auth-loading"><span>R</span></main>;return <CrmShell userName={userName}><div className="content job-detail"><div className="job-detail-head"><div><p className="eyebrow">JOB WORKSPACE</p><h1>{job.title}</h1><p>{job.clients?.first_name} {job.clients?.last_name} · {job.properties?.address_1}, {job.properties?.city}</p></div><span className="stage blue">{job.stage.replaceAll("_"," ")}</span></div><div className="detail-grid"><section className="panel"><div className="panel-head"><div><h3>Job overview</h3><p>The operational source of truth</p></div></div><div className="overview-grid"><div><small>Client</small><b>{job.clients?.first_name} {job.clients?.last_name}</b><p>{job.clients?.email}<br/>{job.clients?.phone}</p></div><div><small>Property</small><b>{job.properties?.address_1||"No property"}</b><p>{job.properties?.city}, {job.properties?.state} {job.properties?.postal_code}</p></div><div><small>Contract value</small><b>${Number(job.contract_value).toLocaleString()}</b><p>Current job value</p></div><div><small>Next action</small><b>{job.next_action||"Not set"}</b><p>Keep the job moving</p></div></div></section><section className="panel"><div className="panel-head"><div><h3>Notes & timeline</h3><p>Every important job event</p></div></div><div className="note-compose"><textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="Add a job note..."/><button disabled={busy||!note.trim()} onClick={addNote}>Add note</button></div><div className="timeline">{activities.map(a=><article key={a.id}><span>✎</span><div><b>{a.kind}</b><p>{a.body}</p><small>{new Date(a.occurred_at).toLocaleString()}</small></div></article>)}{!activities.length&&<div className="empty">No activity yet.</div>}</div></section><section className="panel files-panel"><div className="panel-head"><div><h3>Documents & photos</h3><p>Private files attached to this job</p></div><button onClick={()=>fileRef.current?.click()}>＋ Upload</button><input ref={fileRef} hidden type="file" onChange={upload}/></div>{files.map(f=><div className="file-row" key={f.id}><span>▤</span><div><b>{f.filename}</b><p>{Math.max(1,Math.round(f.size_bytes/1024))} KB · {new Date(f.created_at).toLocaleDateString()}</p></div></div>)}{!files.length&&<div className="empty">No documents or photos yet.</div>}</section></div></div></CrmShell>}
